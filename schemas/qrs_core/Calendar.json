{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Calendar",
  "type": "object",
  "required": [
    "extra_business_days",
    "extra_holidays",
    "valid_from",
    "valid_to"
  ],
  "properties": {
    "extra_business_days": {
      "description": "The extra business days of the calendar. These days are business day weekends.",
      "type": "array",
      "items": {
        "type": "string",
        "format": "date"
      }
    },
    "extra_holidays": {
      "description": "The extra holidays of the calendar. These days are non-business day weekdays.",
      "type": "array",
      "items": {
        "type": "string",
        "format": "date"
      }
    },
    "valid_from": {
      "description": "The valid period of the calendar. include `valid_from`.",
      "type": "string",
      "format": "date"
    },
    "valid_to": {
      "description": "The valid period of the calendar. exclude `valid_to`.",
      "type": "string",
      "format": "date"
    }
  }
}